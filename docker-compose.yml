version: '3.8'

services:
  web:
    build: .
    ports:
      - "8981:80"
    volumes:
      - ./src:/var/www/html
    environment:
      - PHP_INI_SCAN_DIR=/usr/local/etc/php/conf.d
    restart: unless-stopped
